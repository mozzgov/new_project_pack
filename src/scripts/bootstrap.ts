/**
 * Bootstrap JavaScript — enable only what you use.
 *
 * Each import pulls in a single plugin AND registers its data-API, so
 * `data-bs-*` attributes work with no manual init. Comment out the plugins
 * you don't need to keep the bundle small (tree-shaking drops the rest).
 *
 * Full list: https://getbootstrap.com/docs/5.3/getting-started/javascript/
 */

import 'bootstrap/js/dist/collapse'; // accordion, collapsible navbar
// import 'bootstrap/js/dist/dropdown';
// import 'bootstrap/js/dist/modal';
// import 'bootstrap/js/dist/offcanvas';
// import 'bootstrap/js/dist/carousel';
// import 'bootstrap/js/dist/alert';
// import 'bootstrap/js/dist/button';
// import 'bootstrap/js/dist/scrollspy';
// import 'bootstrap/js/dist/tab';

// Tooltip & Popover are opt-in (they must be instantiated manually):
// import { Tooltip } from 'bootstrap';
// document.querySelectorAll('[data-bs-toggle="tooltip"]').forEach((el) => new Tooltip(el));
